spring:
  application:
    name: api-gateway
env: dev
zuul:
  routes:
    user: /user/**
    order: /order/**
    product: /productServer6/**
#   ignored-patterns:
#     - /**/product/listForOrder
    productServer:
      path: /productServer/**
      serviceId: product
      sensitiveHeaders: 
