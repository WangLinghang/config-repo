spring:
  application:
    name: api-gateway
env: dev
zuul:
  routes:
    user: 
      path: /user/**
      serviceId: user
      sensitiveHeaders: 
    order: /order/**
    product: /productServer6/**
#   ignored-patterns:
#     - /**/product/listForOrder
    productServer:
      path: /productServer/**
      serviceId: product
      sensitiveHeaders: 
